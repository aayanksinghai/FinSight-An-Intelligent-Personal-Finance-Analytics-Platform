---
# ── Budget Service ───────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-service
  namespace: finsight
  labels:
    app: budget-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: budget-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: budget-service
    spec:
      containers:
        - name: budget-service
          image: aayanksinghai/finsight-budget-service:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: finsight-config
          env:
            - name: DB_USER_URL
              valueFrom:
                secretKeyRef:
                  name: finsight-secrets
                  key: DB_USER_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: finsight-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: finsight-secrets
                  key: DB_PASSWORD
            - name: JWT_ISSUER
              valueFrom:
                secretKeyRef:
                  name: finsight-secrets
                  key: JWT_ISSUER
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: finsight-secrets
                  key: JWT_PUBLIC_KEY
            - name: SPRING_FLYWAY_TABLE
              value: flyway_schema_history_budget
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 40
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 25
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: budget-service
  namespace: finsight
spec:
  selector:
    app: budget-service
  ports:
    - port: 8084
      targetPort: 8084
